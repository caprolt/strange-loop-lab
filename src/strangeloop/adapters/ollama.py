def generate(system_prompt: str, user_prompt: str, model: str, settings: dict) -> tuple[str, dict]:
    del system_prompt, user_prompt, model, settings
    return "[ollama stub] Configure local runtime integration.", {"input_tokens": None, "output_tokens": None}
